# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: openai-plugin-starter
services:
  api:
    project: .
    language: py
    host: containerapp
    module: app/api
    docker:
      path: ../Dockerfile
      context: ../
      platform: amd64